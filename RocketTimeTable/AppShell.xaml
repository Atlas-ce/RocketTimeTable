<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RocketTimeTable.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:RocketTimeTable"
    Shell.FlyoutBehavior="Disabled"
    BackgroundColor="White"
    >
    <TabBar>
        <!--<Tab Title="Home">
            <ShellContent Title="Day" ContentTemplate="{DataTemplate local:MainPage}" />
        </Tab>
        <Tab Title="Week">
            <ShellContent Title="Week" ContentTemplate="{DataTemplate local:WeekPage}" />
        </Tab>
        -->
        <Tab Title="当天">
            <ShellContent Title="User" ContentTemplate="{DataTemplate local:DayPage}" />
        </Tab>
        <Tab Title="周">
            <ShellContent Title="User" ContentTemplate="{DataTemplate local:WeekPage}" />
        </Tab>
        <Tab Title="设置">
            <ShellContent Title="User" ContentTemplate="{DataTemplate local:UserSettingPage}" />
        </Tab>
    </TabBar>
</Shell>
